package ch.vergasta.plant

import io.flutter.embedding.android.FlutterFragmentActivity

/**
 * Avec le SDK des Home APIs : la variante Fragment, qui est un
 * `ActivityResultCaller`. La demande d'autorisation de Google Home en a
 * besoin pour rendre sa réponse à l'application.
 */
open class HostActivity : FlutterFragmentActivity()
